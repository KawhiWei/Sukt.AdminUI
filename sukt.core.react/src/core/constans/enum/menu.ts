export enum EMenuType {
  Menu = 0,
  Tab = 5,
  Button = 10
}
export enum EMenuMode {
  vertical = "vertical",
  horizontal = "horizontal"
}

export enum EMenuTheme {
  light = "light",
  dark = "dark",
  primary = "primary"
}
